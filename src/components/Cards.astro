---
const { offres } = Astro.props;
---

<div
    class="offre bg-amber-700 w-100 m-4 rounded-2xl flex items-center flex-col"
    data-favori={offres.favori.toString()}
>
    <h2 class="font-bold text-2xl">{offres.nomMaison}</h2>
    <p>{offres.adresse}</p>
    <p>{offres.nbChambres}</p>
    <p>{offres.prix} $$$</p>
    <p>{offres.favori}</p>
</div>
